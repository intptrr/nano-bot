[project]
name = "nano-bot"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "aiogram>=3.28.2",
    "aiohttp>=3.13.5",
    "apscheduler>=3.11.2",
    "python-dotenv>=1.2.2",
    "tzdata>=2026.2",
    "yfinance>=1.4.0",
]

[project.scripts]
nano-bot = "nano_bot.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nano_bot"]
